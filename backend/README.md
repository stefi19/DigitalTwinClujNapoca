# Backend

FastAPI backend that listens for MQTT incidents and exposes a simple HTTP API.

Endpoints:
- GET /health
- GET /incidents

This is a minimal prototype. It keeps incidents in memory for demo purposes.
